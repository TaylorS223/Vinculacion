/**
 * Interfaz que define la información de la universidad
 */
export interface UniversityInfo {
  readonly name: string;
  readonly shortName: string;
  readonly url: string;
  readonly address: string;
  readonly location: string;
}
