import { Component } from '@angular/core';

@Component({
  selector: 'app-topbar',
  template: `<header class="topbar">Barómetro Institucional</header>`,
  styles: '.topbar { font-weight: 600; }',
})
export class TopbarComponent {}
